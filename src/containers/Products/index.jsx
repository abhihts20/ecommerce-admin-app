import React from 'react';
import Layout from "../../components/Layout";

const Products = () => {
    return (
        <Layout sidebar>
            <p>Products</p>
        </Layout>
    );
};

export default Products;
